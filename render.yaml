services:
  - type: web
    name: ChessServer
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python ws_relay_server.py
    envVars:
      - key: PORT
        value: 10000
